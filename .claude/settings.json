{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command // \"\"' | { read -r cmd; if echo \"$cmd\" | grep -qE 'db\\.sqlite3' && echo \"$cmd\" | grep -qE '\\b(rm|unlink)\\b'; then echo '{\"continue\": false, \"stopReason\": \"Blocked: deleting db.sqlite3 is not allowed.\"}'; fi; }",
            "statusMessage": "Checking for protected file operations…"
          }
        ]
      }
    ]
  }
}
